function index() {
  for (let i = 0; i < 10; i++) {
    if (i % 2 == 0) {
      document.write("number:" + i + "<br>");
    } else {
      document.write("number:" + i + "<br>");
    }
  }
}

index();
